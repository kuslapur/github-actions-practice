name: custome env variable

on:
  push

env:
  APP_ENV: production

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - name: Print environment variable
        run: echo  $APP_ENV